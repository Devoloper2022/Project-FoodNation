package com.example.project1.CustomTemplate;

import com.example.project1.Domain.ImageModel;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

//@Repository
//public interface ImageModelRepository extends JpaRepository<ImageModel,Long> {
//    Optional<ImageModel> findImageByUserId(Long id);
//    Optional<ImageModel> findImageByOrgId(Long id);
//    Optional<ImageModel> findImageByFoodId(Long id);
//}
